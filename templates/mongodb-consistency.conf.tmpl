{% if mongodb-consistency.nodes is defined %}
mongodb.url = {%for node in mongodb-consistency.nodes -%}
	                {{ node.server.host.name }}
	                    {%- if not loop.last -%} 
	                        {{ ' '}} 
	                    {%- endif -%}
                 {%- endfor %}
mongodb.database = {{table}}-consistency
mongodb.maxconnections = 100
{% endif %}